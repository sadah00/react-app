REACT JS
-> Is a free and open source JS library used for building interactive UIs specifically for SPAs
-> SPA - Single Page Application
-> It is maintained by Meta

       Background
*SPA - an application that loads a single html page and dynamically updates the content as a user interacts with it -> No reloading of the whole page
*MPA - Multiple Page Application - each page is a separate HTML file and it reloads the whole page -> full page reloads -> browser requests a new page from the server  -. typically slower than an SPA

client(ui) -> Server (api) -> db(storage)

             CORE CONCEPTS
1.Component-based architecture - allows you to break down UI into small reusable pieces called components
 -> Each component has:
    *Logic - JS - manages content 
    *UI - appearance
2.JSX
    -> A syntax extension for JS used in React to describe what the the UI     should look like -> allows you to write HTML like code inside JS
3.Virtual DOM

               Understanding the DOM
DOM  -> Document Object Model - is tree like representation of a webpage. It represents all the elements in a webpage
-> Each element ,attrribute and text becomes a node in the DOM tree

 <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
        <p id='hello'>Hello World</p>
    </div>
    
</body>
</html>

document
            html 
                   head
                   body
                          div
                              p
                                - Hello World
                              p
                                - this is react

1.Create HTML page with elements and attributes
2.Open your page in the browser
3.Browser parses your HTML and CSS and creates a DOM tree
4.JS can then: access elements , change content, modify css,
 add or remove elements dynamically => making pages interactive

       Creating the DOM
1.Browser receives HTML - read HTML line by line
2.Parsing HTML - browser parses HTML,each HTML tag is identified,
  text and attributes also identified, browser creates a node from each element ---> the collection of nodes form a tree like structure called DOM
3.Browser constructs the DOM tree  - JS can access and update the DOM
using various methods like document.getElementByID etc -> creates a dynamic page


Virtual DOM
- A light weight copy of a real DOM  which exists in memory
- A draft version of your webpage

Process
1.Initial Render 
-> React builds a virtual DOM tree from your React code(jsx)
-> It then creates the real DOM from your virtual DOM
2.Data changes 
 -> React creates a new virtual DOM with updated data
3.Reconciliation
 -> React compares new virtual DOM with the previous virtual DOM
-> React only identifies the parts that changed
4.Update the real DOM
-> React updates only the parts that changed

==> Minimize operations and keeps the UI fast

Scaffolding A React project using npm
install node
in your terminal run: npm create vite@latest project_name
run your app: npm run dev

Scaffolded project Structure
1.node modules - where installed packages (React) live
2.public folder - contains static files
      *index.html - single HTML page React injects content into
       - images , favicons,icons,txt files,videos
3.src - contains all your React code
      App.jsx - starting point for your UI
      main.jsx - entry point - renders App.jsx to the DOM
      index.css /App.css - global css files
      assets - images ,icons & other static files for your components

4.package.json - list project dependencies (libraries)
5.package-lock.json - locks the exact version of your dependencies
6. .gitignore - lists files to be ignored by Git
7.vite.config.js - configures how vite builds and serves your React project
8.eslint.config.js - configures ESlint (a tool that checks your code for errors)


Functions - reusable blocks of code used to perform specific tasks

syntax

function functionName (){
    return some_result
}

function greet(){
    return "Hello World"
}

*Arrow Functions*
- shorter way of writing JS functions

const greet = () => {
    return "Hello World"
}

const add = (a,b) => {
    return a + b
}


COMPONENTS
-Reusable piece of UI
-Usually a JS function that returns JSX
-It is the building block of a React App
          Types of Components 
1.Functional Components(most common)   2.Class Components

Functional Components are created using JS ftunctions 
Component names must start with uppercase --- good practice is to have your Component names
 in title case e.g Header.jsx, Navbar.jsx, Card.jsx


Parent Component - a component that contains or renders other Components
Child Component - a component that is rendered or used inside another component


Passing Data From Parent to Child 
Parent components can send data to child components using props
-> Props are like arguments to a function
-> Props are passed down from parent to child
-> Child components recieve props and destructure them to display data from parent 
N/B: Child components cant modify props, props  are read only 


Components - small pieces of reusable UI
Build individual & reusable components
  -> ProductsForm.jsx
  -> Card.jsx
  -> Hero.jsx
Build pages  - pages are still components 
  -> Import multiple components that build a full page inside your pages
  -> Products.jsx - ProductHero.jsx, ProductCard.jsx , Table.jsx



              STATE IN React
-> React doesnt watch normal variables 
-> Changing normal variables in react doesnt trigger a re-render 
-> React needs a special variable that it tracks
-> This special variable is called state
*State* - data that React remembers and uses to update the UI
        - it's like a component's memory
-> When state changes , React updates the page 
-> To create state in React we use what we call a useState hook
-> useState is imported from react
-> useState return two things:
 1.state value
 2.setter function - function used to update state

-> Inside useState() we have the initial value

<a href="/products"> Products </a>


*Routing in React*
-> In React routing is enabled by React-Router
-> React Router is a library that allows React apps to handle navigation and URLs -> lets you show different components
 for different paths without page reloads

npm install react-router-dom

Browser Router -> wraps your React application to enable routing
Routes - container for all Route Component
Route - defines a path and component to render
Link - naviagtion element - replaces anchor tag
 

 Layout.jsx -> contains all common features of every page e.g. Navbar & Footer
 Outlet - a placeholder element for every component that is wrapped in layout.jsx -> every single page 
    that has access to navbar and footer

COMPONENT LIFECYCLE
-> describes stages a component goes through from the moment it appears on screen until it disappears
1.Mounting Phase
 -> component appears
 -> component is created and inserted into the dom
2.Updating Phase
 -> when a component re-renders
 -> happens when: state changes,props change, parent re-renders
3.Unmounting Phase
-> Component is removed from the DOM 


HOOKS
-> Functions that let you use React features(state,lifecycle,context etc) inside functional components
-> hooks start with use keyword

Rules on using hooks
1.used at top level only
2.only call hooks inside functional components

-> useState ->useEffect -> useContext ->useRef -> useMemo ->useCallback ->Custom hooks

 useState - used to track and update state
 useEffect -> lets you run sideffects in React components
    *sideffects* ->something outside rendering e.g.API calls,fetching data, 
            event listeners, timers, logging

useContext -> lets a component access global state without prop drilling
   e.g passing token across pages , dark mode vs light mode
 

Dependency array - determines how many times a sideeffect happens
1.when there is no Dependency array -> effect runs after every render
2.when empty - no dependencies
             - effect runs only once , when component first mounts
3.passing dependencies - [productData] -> run every time productData changes

